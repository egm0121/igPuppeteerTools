{
  "name": "igpopularfollowers",
  "version": "1.0.0",
  "description": "-use phantomjs or chrome headless - scrape foreach insta profile handle the profile page - execJs to grab the follower count : '_sharedData.entry_data.ProfilePage[0].graphql.user.edge_followed_by.count'",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^1.7.0"
  }
}
